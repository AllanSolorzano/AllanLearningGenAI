apiVersion: v1
kind: Secret
metadata:
  name: chaos-db-secret
  namespace: chaos-app
type: Opaque
stringData:
  # TODO: Replace before applying manually. deploy-apps.sh can create a demo secret from env vars.
  DB_USER: "postgres"
  DB_PASSWORD: "REPLACE_ME_WITH_A_DEMO_OR_EXTERNAL_SECRET_VALUE"
  POSTGRES_USER: "postgres"
  POSTGRES_PASSWORD: "REPLACE_ME_WITH_A_DEMO_OR_EXTERNAL_SECRET_VALUE"
